import { ADD_TODO, TOGGLE_TODO } from "../actionTypes";

// normalized reducer of todos

const initialState = {
  allIds: [],
  byIds: {}
};

// appends id upon receiving ADD_TODO action OR
// toggles completed field upon receiving TOGGLE_TODO action
export default function(state = initialState, action) {
  switch (action.type) {
    case ADD_TODO: {
      const { id, content } = action.payload;
      return {
        ...state,
        allIds: [...state.allIds, id],
        byIds: {
          ...state.byIds,
          [id]: {
            content,
            completed: false
          }
        }
      };
    }
    case TOGGLE_TODO: {
      const { id } = action.payload;
      return {
        ...state,
        byIds: {
          ...state.byIds,
          [id]: {
            ...state.byIds[id],
            completed: !state.byIds[id].completed
          }
        }
      };
    }
    default:
      return state;
  }
}
